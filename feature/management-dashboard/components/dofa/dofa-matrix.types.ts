export type MatrixCellKey = `${string}__${string}`;

export type ItemDraft = {
  perspective: string;
  category: string;
  description: string;
  priority: string;
  impactLevel: string;
  order: number;
  responsibleId: string;
};

